# Цикл 42 — отчёт FaceWatch

**Сессия:** `2026-08-18T101100Z`
**Ветка:** `claude/pensive-brown-28588t`
**Версия SPEC.md:** «Production NVR Edition», 422 строки; последнее
изменение — `8ffc46d` от 2026-08-16T20:06:58Z, без изменений с цикла 32

---

## Итог цикла

**`Skipped: concurrent session still active`.**

Цикл остановлен на шаге 1 протокола — на проверке конкурентности, **до**
drift check, feature gap analysis, аудита и любых замеров. **0 PR из 10,
код не менялся, ни одной ветки не создано, зависимости не ставились.**
Carryover цикла 40 наследуется целиком и не тронут.

Содержательных результата два, и оба не по коду:

1. **Проверка по возрасту коммита дала ложноотрицательный результат, а
   поймала пересечение проверка по открытым PR** — впервые эти две
   половины протокола разошлись в разные стороны на одном и том же
   пересечении (раздел 2).
2. **Разбор истории вскрыл ещё одно незамеченное пересечение — циклов 40
   и 41** (раздел 3), и заодно то, что коммит-отчёт цикла 40 **потерял
   штамп `[session:...]`** — то есть маркер, введённый специально для
   обнаружения пересечений, отсутствовал ровно на том коммите, по
   которому следующая сессия и должна была это пересечение увидеть.

Обе улики отправлены комментарием в issue
[#56](https://github.com/IMBANEKONTRITSYA/Claude-NVR/issues/56).

---

## 1. Sanity check ветки (шаг 0) — пройден

`main` в репозитории — заглушка (единственный файл `1`), вся работа живёт
в `claude/pensive-brown-28588t`; это штатное состояние, а не повреждение.
На рабочей ветке на месте: `SPEC.md` (422 строки), `docker-compose.yml`,
`docs/reviews/REVIEW_LOG.md`, `docs/DEPLOY_CHECKLIST.md`,
`.github/workflows/`, каталоги `backend`, `worker`, `frontend`,
`upscaler`, `mediamtx`, `perf`, `loadtest`, `scripts`. Плейсхолдеров нет.

Assigned harness branch этой сессии — `claude/optimistic-franklin-lljh9s`
— проигнорирована по инструкции; её содержимое — копия заглушки `main`.

## 2. Concurrency check (шаг 1) — СТОП

```
git fetch origin
git log --oneline origin/claude/pensive-brown-28588t -10
```

| Величина | Значение |
|---|---|
| Время проверки | 2026-08-18T10:12:02Z |
| Последний коммит ветки | `b5687a1` — отчёт цикла 40, **без штампа `[session:...]`** |
| Время коммита | 2026-08-18T09:23:51Z |
| **Возраст коммита** | **48 мин 11 с** (порог 30 мин) → **проверка пройдена** |
| Открытый PR рутины | [#117](https://github.com/IMBANEKONTRITSYA/Claude-NVR/pull/117), база — рабочая ветка, draft |
| Создан | 2026-08-18T09:49:20Z — **возраст 22 мин 42 с** (порог 30 мин) → **СТОП** |
| Головной коммит #117 | `1ec97b9`, 2026-08-18T10:08:28Z — **возраст 3 мин 34 с** |
| Штамп сессии-владельца | `[session:2026-08-18T092000Z]` |
| CI по головному коммиту | `state: pending`, `total_count: 0` — не запускался |
| `mergeable_state` | `unstable` |

**Владелец жив и работает прямо сейчас.** Второй коммит в PR (`1ec97b9`)
— правка по результатам прогона CI («сравнение режимов было нечестным на
SMT-раннере»), то есть сессия `092000Z` только что получила красный/шумный
результат, исправила его и ждёт нового прогона. На момент проверки она в
работе не менее **52 минут** (старт 09:20Z).

**Новое здесь — расхождение двух половин проверки.** Протокол
предписывает две: возраст последнего коммита ветки и возраст открытого PR
рутины. Раньше они либо обе молчали (ложноотрицательные результаты циклов
20 и 31), либо срабатывали заодно. Здесь **коммит-проверка пропустила
пересечение с запасом в полтора порога** (48 мин при пороге 30), потому
что чужая сессия пишет не в рабочую ветку, а в свою — и рабочая ветка
стоит нетронутой ровно до мерджа. Пересечение поймала **только** вторая
половина. Вывод для #56: проверка «возраст последнего коммита рабочей
ветки» структурно слепа к нормальному режиму работы сессии — от старта до
мерджа первого PR (здесь 29+ минут, у цикла 34 — более трёх часов)
рабочая ветка не меняется вовсе, и порог в 30 минут по ней не значит
ничего. Единственный работающий индикатор — открытый PR с базой на
рабочей ветке.

## 3. Незамеченное пересечение циклов 40 и 41

Разбор истории для этой проверки дал вторую улику — уже не о текущем
пересечении, а о предыдущем, которое никто не зафиксировал.

| Время (UTC) | Событие | Сессия |
|---|---|---|
| 06:16:00 | старт цикла 40 | 40 = `2026-08-18T061600Z` |
| 09:20:00 | **старт цикла 41** | 41 = `2026-08-18T092000Z` |
| 09:23:51 | коммит `b5687a1` — отчёт цикла 40, терминальное действие протокола | **40** |
| 09:46:28 | создание ветки `perf/numa-analytics-affinity-...` | 41 |
| 09:47:49 | первый коммит `76bc4bd` | 41 |
| 09:49:20 | PR #117 | 41 |
| 10:08:28 | коммит `1ec97b9` (правка по CI) | 41 |
| 10:11 | старт цикла 42 (этот) | 42 |

Пересечение 40/41 — **не менее 3 мин 51 с**: цикл 41 стартовал, пока цикл
40 ещё писал отчёт. Длительность цикла 40 (06:16 → 09:24 = **3 ч 07 мин**)
оказалась больше интервала до старта цикла 41 (**3 ч 04 мин**) — то есть
пересечение было арифметически неизбежным, а не случайным.

**Интервалы между стартами и длительности циклов** (обновление таблицы
цикла 33):

| Переход | Интервал | Длительность предыдущего цикла | Пересечение |
|---|---|---|---|
| 37→38 | 1 ч 03 мин | 27 мин | нет |
| 38→39 | 3 ч 58 мин | 51 мин | нет |
| 39→40 | 2 ч 06 мин | 1 ч 20 мин | нет |
| 40→41 | 3 ч 04 мин | **3 ч 08 мин** | **да, ≥ 3 мин 51 с** |
| 41→42 | **51 мин** | ≥ 52 мин (не завершён) | **да, ≥ 52 мин** |

Разброс длительностей 27 мин … 3 ч 08 мин при разбросе интервалов 51 мин
… 3 ч 58 мин: фиксированный интервал задачу по-прежнему не решает
(рекомендация 2 из #56 потребовала бы 4+ часов), рекомендация 1
(распределённый лок с маркером присутствия и TTL) остаётся единственной
закрывающей.

**Отдельно — деградация самого механизма обнаружения.** Коммит `b5687a1`
(отчёт цикла 40) **не несёт штампа `[session:...]`**, хотя протокол
требует его на каждом коммите, а issue #56 прямо называет этот штамп
средством, ради которого он и введён. Штампа нет ровно на том коммите,
который следующая сессия читает первым: цикл 41, глядя на тип ветки, не
мог отличить «отчёт чужой сессии, написанный четыре минуты назад» от
своего же будущего состояния. Это не причина пересечения 40/41 (там хватило
арифметики интервалов), но это отказ единственного дешёвого индикатора.
Проверяется:

```bash
git log -1 --format=%B b5687a1 | grep -c 'session:'   # 0
```

## 4. Что НЕ делалось

Прямые следствия остановки на шаге 1 — записаны, чтобы отчёт не читался
как «проверено и чисто»:

* **Drift check SPEC.md не выполнялся** (сверены только редакция и размер
  файла — 422 строки, последнее изменение `8ffc46d` от 2026-08-16T20:06:58Z;
  ключевые параметры — 120 камер, два слоя, режимы камер, целевой сервер,
  MediaMTX — с кодом не сверялись).
* **Feature gap analysis §3-13 не обновлялся** — таблица гэпов цикла 40
  действует как есть.
* **Ни одного норматива §26 не измерено.** Список «не измерено» из
  carryover цикла 40 не тронут; правило «один неизмеренный норматив за
  цикл» в этом цикле не исполнено.
* **Зависимости не ставились**, сьюты не гонялись, бенчмарки не
  запускались — по той же причине: любой прогон на общей ветке в момент,
  когда её ждёт чужой мердж, только добавляет шума в сравнение чисел.
* **PR #117 не рецензировался и не мерджился.** Рекомендация 3 из #56
  (мерджить только собственные PR, сверяя `[session:...]`) соблюдена:
  штамп `092000Z` — чужой.

## 5. Carryover

Наследуется целиком из отчёта цикла 40, без изменений и без переоценки:
§19 «восстановление потока ≤ 5 с» не выполняется (6.6 с, решение
архитектурное); Profile G Replay; §26 .deb; хардкод числа камер;
отсутствие клиентских keepalive у asyncpg; список неизмеренных нормативов
§26.

**Добавлено этим циклом (P1, оркестрация, не код):**

1. Проверка «возраст последнего коммита рабочей ветки» признана структурно
   слепой к живой сессии до её первого мерджа — единственный работающий
   индикатор — открытый PR с базой на рабочей ветке. Формулировку шага 1
   протокола стоит поправить у автора расписания.
2. Штамп `[session:...]` на коммите-отчёте пропущен циклом 40; стоит
   сторожить его тем же способом, каким сторожатся ссылки на разделы ТЗ.

## 6. Известные гэпы — без изменений

Список «проверить на сервере» из `docs/DEPLOY_CHECKLIST.md` в этом цикле
не пересматривался.
